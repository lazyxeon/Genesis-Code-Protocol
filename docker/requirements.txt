# Core CLI/runtime deps (safe across Linux runners)
click==8.1.7
rich==14.1.0
pyyaml==6.0.2
requests==2.32.5  # fixed .netrc credential leak (CVE-2024-35195)
numpy==2.2.6
pandas==2.2.2

# Optional — enable as needed (uncomment to use)
# pyspark==3.5.1              # Spark (CPU)
# delta-spark==3.2.0          # Delta Lake (match to Spark 3.5.x)
# fastapi>=0.111.0            # if you run the example API
# uvicorn[standard]>=0.30.0   # if you run the example API
# scikit-learn>=1.5.1         # if you do ML baselines
# networkx>=3.3               # if using graph-enhanced reasoning in code
# matplotlib>=3.9.0           # if plotting in CLI/notebooks
